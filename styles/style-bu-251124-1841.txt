/* Extracted/centralized styles from index.html */

/* Font-loading animation originally in head */
@keyframes fonts-loading {
  0%, 99% {
    color: transparent;
  }
}
html.wf-loading * {
  animation: fonts-loading 3s;
}

/* Small hamburger/burger lines */
.top-bun,
.patty,
.bottom-bun {
  height: 1px;
}

/* Preserved @supports block from original (was empty in original file).
   Add backdrop-filter or other styles here if you want header blur behavior. */
@supports (-webkit-backdrop-filter: none) or (backdrop-filter: none) {
  .header-blur-background {
    /* intentionally left blank (original contained an empty block) */
  }
}

/* Moved inline styles for heading and paragraphs */
.intro-heading {
  text-align: center;
  white-space: pre-wrap;
}

.intro-paragraph {
  white-space: pre-wrap;
}